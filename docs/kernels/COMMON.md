# Shared conventions for the kernel workstreams (M5 Max 40-core, 128 GB, macOS 26.5, mlx 0.32.2)

Python: ~/inference-server/kdev/bin/python  (mlx 0.32.2, mlx-lm 0.31.3). Do NOT touch /Applications/oMLX.app or the running daemon (port 8083, serves a live user). GPU is shared: keep benchmark runs short (median of ~10 iterations, chain 10 ops per mx.eval to amortise submission; see ~/inference-server/kbench.py).
Kernel mechanism: JIT Metal via mx.fast.metal_kernel(name, input_names, output_names, source, header=...) — no MLX rebuild. Study these oMLX patches as templates (read-only): /Applications/oMLX.app/Contents/Resources/omlx/patches/{qwen35_verify_qmm.py,qwen35_moe_gate_up.py,qwen35_moe_weighted_sum.py,m5_gather_qmm.py,qwen35_q4_mlp.py,qwen35_ragged_decode.py}. Deliver your kernel as a patch-style module: a function that monkeypatches the relevant mlx / mlx-lm call, with an environment toggle, and a clean fallback to the stock path for unsupported shapes/dtypes.
Target model Qwen3.8-Flash-Next (oQ4e): hidden 2560; attention q_proj N=6144 K=2560, o_proj N=2560 K=6144, kv N=512; 512 routed experts top-10 with up/gate [512,640,2560] and down [512,2560,640]; shared expert 640; lm_head N=248320 K=2560; affine 4-bit group 64 (also 5/6/8-bit tensors exist; 4-bit gs64 first, 8-bit gs64 second). MTP verify runs M=2 rows; batched decode M=2..8; prefill chunks 2048 tokens (40 rows/expert).
Baseline (stock mlx 0.32.2, measured today, per-kernel): copy ceiling 549 GB/s. Dense q_proj: M=1 0.027ms 332GB/s; M=2 0.029; M=4 0.037; M=8 0.053; M=32 0.107; M=2048 1.17ms 55 TFLOPS. shared mlp N=640: M=1 0.021ms (44 GB/s, launch-bound). lm_head: M=1 0.454ms; M=8 3.55ms (102 GB/s). MoE gather_qmm up-proj: T=1 0.035ms 261GB/s; T=2 0.043; T=8 0.128; T=2048 sorted 0.655ms ~100 TFLOPS (unsorted 29.5ms).
Correctness bar: fp32 accumulation; output must match mx.quantized_matmul / mx.gather_qmm to within bf16 tail-ULP (report max abs and rel error on random and on real-weight-like distributions) and greedy argmax must agree on a 1000-row lm_head sample. Any mismatch beyond that is a bug, not a tolerance.
Deliverables in your directory: kernel.py (patch module), test_exact.py, bench.py, REPORT.md (what you built, correctness table, before/after speed table with the exact shapes above, how to enable in oMLX, limitations, honest failures, next steps). Time-box: deliver a working increment with numbers rather than a perfect kernel with none.
